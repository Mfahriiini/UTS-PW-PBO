<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        /* Background Styling */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #fff;
            background: url('https://wallpapercave.com/wp/wp2046111.jpg') no-repeat center center/cover;
            position: relative;
            overflow: hidden;
        }

        /* Main Container */
        .login-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            background: rgba(0, 0, 0, 0.6);
            padding: 20px 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 255, 255, 0.5);
        }

        /* Neon Effect for Heading */
        h2 {
            font-size: 2.5rem;
            color: rgb(31, 45, 45);
            text-shadow: 0 0 10px rgb(101, 186, 201), 0 0 20px rgb(124, 158, 204), 0 0 30px rgb(127, 199, 241), 0 0 40px #0ff;
            animation: neon-flicker 1.5s infinite alternate;
            margin-bottom: 20px;
        }

        /* Form Styling */
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            width: 100%;
        }

        input[type="email"],
        input[type="password"] {
            padding: 10px;
            font-size: 1rem;
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
            outline: none;
            transition: all 0.3s ease;
        }

        input[type="email"]:focus,
        input[type="password"]:focus {
            background: rgba(255, 255, 255, 0.2);
            box-shadow: 0 0 15px #0ff, 0 0 30px #0ff;
        }

        /* Button Styling */
        button {
            padding: 10px;
            font-size: 1.2rem;
            color: #000;
            background: rgb(80, 255, 255);
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 0 10px rgb(88, 182, 182), 0 0 20px #0ff;
        }

        button:hover {
            color: #0ff;
            background: #000;
            box-shadow: 0 0 20px #0ff, 0 0 40px #0ff;
            transform: scale(1.05);
        }

        /* Registration Link Styling */
        .register-link {
            margin-top: 15px;
            color: rgb(255, 146, 74); /* Neon yellow */
            font-size: 1rem;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .register-link:hover {
            color: #0ff;
            text-shadow: 0 0 5px #0ff, 0 0 10px #0ff;
        }

        /* Keyframe Animations */
        @keyframes neon-flicker {
            0% { opacity: 1; }
            50% { opacity: 0.7; }
            100% { opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Login</h2>
        <form action="/login" method="POST">
            <input type="email" name="email" placeholder="Email" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Login</button>
        </form>
        <!-- Registration Link -->
        <a href="/register" class="register-link">Don't have an account? Register here</a>
    </div>
</body>
</html>
